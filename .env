# Flask environment settings
FLASK_ENV=development
SECRET_KEY=your-secret-key-for-dev

# PostgreSQL (local or Azure PostgreSQL Flexible Server)
DATABASE_URL=postgresql://postgres:root@localhost/collectivercm

# MongoDB (local or Azure Cosmos DB with MongoDB API)
MONGO_URI=mongodb://localhost:27017

# Redis (used by Flask-Limiter)
REDIS_URI=redis://localhost:6379

# Email/SMTP (used for password reset, verification, etc.)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=thanuka.ellepola@gmail.com
MAIL_PASSWORD=nrxm aiug aiat oxiq
MAIL_DEFAULT_SENDER=thanuka.ellepola@gmail.com

MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=collective_rcm
